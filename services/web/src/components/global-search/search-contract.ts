/**
 * Leaf constants shared by the global-search dialog. Kept import-free (no
 * components) so it can be depended on without cycles.
 */

/** Minimum query length before search fires (the nav filter + `search.global`). */
export const SEARCH_MIN_QUERY = 2;
