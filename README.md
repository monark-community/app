# Assets: auth gradient backdrop

Screenshots referenced from the pull request for `feat/auth-gradient-backdrop`.
They live on this detached branch so the feature branch's diff stays code-only.
Captured with `pnpm screenshots auth-screen --dark`, downscaled to 1x.
