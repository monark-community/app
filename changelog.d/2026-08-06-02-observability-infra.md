- 2026-08-06: Observability / infra — **Sentry error tracking on both services, gated purely on a DSN env var** (no feature flag ; unset = a complete no-op, so local dev and any deploy that hasn't opted in are unaffected). **API** (`@sentry/node`): a first-line `import "./instrument"` in [server.ts](monark/app/services/api/src/server.ts) inits Sentry before express / http are imported so auto-instrumentation wraps them ([instrument.ts](monark/app/services/api/src/instrument.ts)) ; the raw Express routes (`/cron`, `/hooks`, `/chat/stream`) are caught by `setupExpressErrorHandler`, and the tRPC `onError` hook reports only genuine server failures (client-class codes like `NOT_FOUND` / `BAD_REQUEST` stay out). **Web** (`@sentry/nextjs`): [instrumentation.ts](monark/app/services/web/src/instrumentation.ts) + [instrumentation-client.ts](monark/app/services/web/src/instrumentation-client.ts) init the server/edge + browser SDKs, and [next.config.ts](monark/app/services/web/next.config.ts) wraps the build with `withSentryConfig`, routing browser events through a **same-origin tunnel** (`/monitoring`) so the app's existing CSP (`connect-src 'self'`) covers them — no CSP change. `sendDefaultPii` is off everywhere (auth tokens + PII live in headers/bodies/cookies) and traces default to sample rate 0 ; web source-map upload runs only when `SENTRY_AUTH_TOKEN` is set (CI), else the build still succeeds. Env vars documented in both `.env.example` files + [observability.md](monark/app/docs/technical-documentation/observability.md). `typecheck` + `lint` (api + web) + a full `web build` green.
