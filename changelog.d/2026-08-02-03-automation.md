- 2026-08-02: Automation — **data-flow follow-ups: variable-input toggle (select/boolean/number), slug-uniqueness validation, and a user guide.** (1) Non-text config fields (a dropdown, switch, or number spinner can't hold a `{{ }}` token) now show a small toggle that swaps in a text box accepting a reference, wired to the click-to-insert picker like any text field ([automation-editor.tsx](<monark/app/services/web/src/app/(authed)/automation/[id]/automation-editor.tsx>) `ConfigField`; forced on when the stored value already is an expression, so a saved reference never hides behind a dropdown with no matching option). Excludes `event-type` (trigger-only, drives the event-fields UI) and `schedule` (structured builder). (2) `validateGraph` gained a `duplicate-slug` error so two nodes sharing a `{{ steps.<slug> }}` address are caught pre-run ([validate.ts](monark/app/packages/automation/src/contracts/validate.ts)) — the editor already prevents dupes, this covers graphs built via the public API; surfaced in the editor's preflight list and the server's `runNow` guard ([router.ts](monark/app/packages/automation/src/server/router.ts)). (3) First **user-facing** doc for the feature: [docs/user-guide/automation-variables.md](monark/app/docs/user-guide/automation-variables.md) (linked from the guide index) explains references, the picker, the value/variable toggle, reference names, and Set Variable — no code, for the person building automations. New i18n keys `useVariable` / `useFixedValue` / `variablePlaceholder` (ICU-escaped literal braces) / `preflight.duplicateSlug`, plus a reworded `variablesHint` (en + fr). New tests: 2 validate-unit (duplicate/unique slug) + 1 integration composing the whole redesign (a `steps.<slug>` output → Set Variable → `{{ vars }}` read downstream). **automation 43 unit + 29 integration green**; `web` typecheck + lint + `check:i18n` green.
