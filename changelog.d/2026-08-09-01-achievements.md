- 2026-08-09: Achievements — **image-only badges** (dropped the emoji). Removed `Achievement.icon` (migration `20260809030000_achievement_drop_emoji`) and the emoji input ; a badge is now solely an uploaded image (`iconFileId`). When none is set the admin list, editor, and gallery render a neutral `Award` placeholder ; the user [gallery](<monark/app/services/web/src/app/(authed)/achievements/achievements-gallery.tsx>) renders a locked (unearned) badge grey-scaled. typecheck + lint + `check:i18n` / `check:tiers` / `check:modules` green ; 7 unit + 4 integration unchanged.
