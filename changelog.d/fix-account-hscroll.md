- 2026-09-08: Web — **remove the horizontal scrollbar on `/account/profile` (and any other `SectionShell` page)**. `UserBanner`'s full-bleed hero sizes itself off the raw viewport (`w-screen` / `50vw`), which assumes it spans the true window width ; once `SectionShell`'s `<main>` needs its own vertical scrollbar, that scrollbar eats a few pixels of `<main>`'s content box the viewport-based math doesn't know about, and the hero overshoots by exactly that width. [section-shell.tsx](services/web/src/components/section-shell.tsx) now clips it with `overflow-x-hidden` on `<main>` rather than re-deriving the bleed math ; safe, since the one thing meant to be wider (`DataTable`'s tables) already scrolls in its own internal wrapper.
