- 2026-09-08: Added Google to social sign-in, on top of the GitHub-first provisioning path in `@monark/auth`; `google` joins [`OAUTH_PROVIDERS`](packages/auth/src/contracts/oauth.ts) with a brand mark in [oauth-buttons.tsx](<services/web/src/app/(anon)/oauth-buttons.tsx>) and an `[auth.external.google]` stanza in [supabase/config.toml](supabase/config.toml) (shipped disabled). No change to the callback, provisioning, refusal or account-page logic — none of it is provider-specific. Google is the one provider here that sends the avatar as `picture` rather than `avatar_url`, which is why `extractOAuthProfile` checks both; a test pins that. Operators should note that app verification is only required for sensitive scopes, so the plain email + profile scopes used here need none, and that a local sign-in failing on a nonce mismatch wants `skip_nonce_check = true` in the local config only.
