- 2026-05-05: Test plan iteration 3. Component-test infrastructure for `services/web` lands ; the security-sensitive surface (interactive islands, server actions) is now testable in jsdom without Docker / DB / SMTP.
  - **Web service test harness.** New [services/web/vitest.config.ts](monark/app/services/web/vitest.config.ts) extends the shared root config with the React plugin, jsdom environment, the `@/` path alias mirroring the app's, and an `include` pattern of `tests/components/**` + `tests/actions/**`. Coverage threshold targets the security-sensitive `src/app/(authed)/**/actions.ts` files at 80 % (per [test-plan.md](monark/app/docs/technical-documentation/test-plan.md)) ; commented off until suites land. Boilerplate setup at [tests/setup.ts](monark/app/services/web/tests/setup.ts) wires `@testing-library/jest-dom`'s matchers + the per-test `cleanup()`.
  - **Shared test utilities.** [tests/test-utils.tsx](monark/app/services/web/tests/test-utils.tsx) exports `<TestIntlProvider>` (preloaded with the real en + fr message catalogs from the codebase, so `useTranslations` resolves to actual production strings — no placeholder keys) and `renderWithIntl(ui, { locale })`. Re-exports the rest of `@testing-library/react` + `userEvent` so tests have a single import path. New [tests/tsconfig.json](monark/app/services/web/tests/tsconfig.json) adds `vitest/globals` + `@testing-library/jest-dom` to the type roots so editor IntelliSense + `tsc` see the matchers.
  - **Three proof-of-concept component tests.**
    - [tests/components/role-chip.test.tsx](monark/app/services/web/tests/components/role-chip.test.tsx) — 9 cases covering presentational + interactive surface : default name rendering, muted fallback when color is null/undefined, color-tint application via inline styles, className merge, conditional remove button, click-fires-onRemove, removeDisabled state.
    - [tests/components/account-sidebar.test.tsx](monark/app/services/web/tests/components/account-sidebar.test.tsx) — 7 cases. Mocks `next/navigation` `usePathname` + `trpc.users.me.useQuery` and verifies the four-tab default layout, deletion-grace filter (only profile + danger visible), `aria-current=page` active-state matching, prefix-match for sub-paths, no-active-tab fallback, undefined-data loading state, and the horizontal-orientation prop. Sets the canonical pattern for component tests that depend on tRPC.
    - [tests/components/app-bar-breadcrumb.test.tsx](monark/app/services/web/tests/components/app-bar-breadcrumb.test.tsx) — 11 cases. Stubs every dynamic-id query (users / orgs / roles) + the bootstrapStatus and walks through the breadcrumb's actual logic : home-route renders nothing, shallow paths render verbatim, > 3 segments collapse with an ellipsis, `/admin` is non-clickable always, `/admin/organizations` is non-clickable in single-tenant mode + clickable in multi-tenant, dynamic ids resolve to display names (user / org / role), the literal `new` segment doesn't trigger the role lookup, and unmapped static segments fall back to title-cased kebab-case.
  - **New devDeps on `services/web`** : `vitest`, `@vitest/coverage-v8`, `@vitejs/plugin-react`, `jsdom`, `@testing-library/{react, jest-dom, user-event}`, `msw` (for future tRPC HTTP stubbing). The `web` package now has `test` + `test:coverage` scripts so `pnpm test` includes the web tests in the workspace run.
  - **Workspace effect** : nothing breaks. The new tests live in `services/web/tests/` which is outside `tsconfig.json`'s `include` (`src/**/*` only), so workspace `pnpm typecheck` + `pnpm lint` stay green. The tests themselves only run after `pnpm install` picks up the new devDeps.
  - **Operator handoff** : `pnpm install` on the next checkout (or `pnpm install --frozen-lockfile=false` first time) pulls the new packages + commits a fresh lockfile. After that, `pnpm --filter web test` runs the three suites ; `pnpm test` includes them in the workspace pass.
