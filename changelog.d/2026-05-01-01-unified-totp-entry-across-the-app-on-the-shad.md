- 2026-05-01: Unified TOTP entry across the app on the shadcn `<InputOTP>` (3-3 grouped slots with separator, already used by signup check-email + forgot-password). Five places switched away from a plain `<Input maxLength={6} placeholder="000000">`: enroll-code on the account TOTP enrollment screen, manage-code in the disable / regenerate flow ([totp-section.tsx](<monark/app/services/web/src/app/(authed)/account/totp-section.tsx>)), the TOTP gates that protect password change ([password-section.tsx](<monark/app/services/web/src/app/(authed)/account/password-section.tsx>)) and email change ([email-change-form.tsx](<monark/app/services/web/src/app/(authed)/account/email-change-form.tsx>)), and the sign-in TOTP challenge ([signin/totp/totp-form.tsx](<monark/app/services/web/src/app/(anon)/signin/totp/totp-form.tsx>)). The recovery-code branch on the sign-in form keeps the regular Input because the 14-char `XXXX-XXXX-XXXX` shape doesn't fit the 6-slot layout. Submit buttons now also gate on `code.length < 6` so the user can't fire an obviously incomplete code at the server. The dev-overlay TOTP panel intentionally keeps its compact `<Input>` ; it's not user-facing.
