- 2026-07-04: Calendar — day view drops the dead date-header row and opens scrolled to now. With calendars merged into one grid, the sticky day-of-week + circled-date header row above the timeline showed nothing useful and just ate vertical height, so it's removed ([day-view.tsx](<monark/app/services/web/src/app/(authed)/calendar/day-view.tsx>)) — the all-day row stays (now the only sticky header, rendered only when there are all-day events). The schedule now starts at the top of the scroll container, so the `COLUMN_HEADER_HEIGHT` offset was zeroed out of every drag / popover-anchor calculation. Opening the day view now scrolls the timeline to the current time (a couple of hours of lead-in above it) on first mount, unless a notification/create deep-link already owns the scroll position. `tsc` + `eslint` green.
