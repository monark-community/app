- 2026-08-09: Shell — **account-menu widget slots**, first consumer is achievements. Added [`config/shell-widgets.tsx`](monark/app/services/web/src/config/shell-widgets.tsx) — the rendered-visual analogue of `PRIMARY_NAV` : a single, ordered, flag-gated config array declaring which module components fill a named shell slot (today `account-menu`), resolved via `useShellWidgets(slot)` off the same `getAllForSession` flag map the nav uses. [`UserMenu`](monark/app/services/web/src/components/user-menu.tsx) renders the slot in place of its three placeholder cards, falling back to them when nothing is registered + enabled. First widget : [`AchievementsMenuWidget`](monark/app/services/web/src/components/achievements/achievements-menu-widget.tsx) — earned-badge count + the latest badge thumbnails, linking to `/achievements`, gated by `achievements.enabled` (so it only mounts + queries where the module is on) ; backed by a new cheap `achievements.summary` tRPC procedure (`achievements.view`, count + latest N with resolved icon URLs). Kept on the repo's established static-config grain rather than a client `register()` (a client registry would still need the host to import each module's side-effect). typecheck + lint + `check:i18n` / `check:tiers` / `check:modules` green.
