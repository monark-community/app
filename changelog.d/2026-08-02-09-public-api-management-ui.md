- 2026-08-02: Public API (management UI) — a new **Account → API keys** tab ([/account/api-keys](<monark/app/services/web/src/app/(authed)/account/api-keys/page.tsx>)) where a member manages their own keys : mint, list, and revoke. Minting opens a dialog for a name, a scope picker (checkboxes driven by `@monark/api-keys`'s `ALL_API_SCOPES` so it tracks the catalog), and an expiry preset (never / 30 / 90 / 365 days) ; on success the plaintext is shown **exactly once** in a copy-to-clipboard reveal dialog with a "you won't see this again" warning, matching the write-once contract ([api-keys-section.tsx](<monark/app/services/web/src/app/(authed)/account/api-keys-section.tsx>)). The list shows each key's name, prefix, scopes, status (active / expired / revoked), and created / last-used / expiry, with a confirm-gated revoke. The whole surface is gated on the `api-keys.manage` permission (a member without it sees an explainer, and the `apiKeys.list` query stays disabled) ; it follows the account-section idiom (`PageSection` + `ConfirmDialog`) with a layout-accurate `loading.tsx` skeleton and a new sidebar entry. Fully localized (new `account.apiKeys.*` + `common.done`, en + fr — `check:i18n` 1832 keys each). Added files typecheck + lint clean ; `check:tiers` / `check:modules` / `check:i18n` green. (Note : the shared `web` typecheck is currently red on unrelated in-flight query-language work in `data/models/[modelKey]`, not from this change.)
