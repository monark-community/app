- 2026-09-07: Docs — **enforce agent git identity in the working agreement**. The `monark-agent` machine account and `MONARK_AGENT_TOKEN` (added 2026-08-16) existed but were never written into [CLAUDE.md](CLAUDE.md), so a session had no way to discover them and defaulted to the operator's own git identity. § Parallel work now spells out the concrete commands — worktree-local `git config user.email` set to the account's noreply address, pushing over HTTPS with the token instead of the operator's SSH key, and opening the PR as `monark-agent` too — plus a hard stop-and-ask if the token isn't set, rather than silently falling back to the operator's identity.
