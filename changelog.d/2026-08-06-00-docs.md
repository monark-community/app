- 2026-08-06: Docs — **documented the CI pipeline and brought the deploy checklist current with this session's infra work.** New [ci.md](monark/app/docs/technical-documentation/ci.md) describes the GitHub Actions verify gate — the `main` + `develop` triggers (the develop gate added this session), each parallel job (lint/typecheck, repo-checks, migration-drift, unit + integration, coverage-upload), the per-package coverage-floor rules (a floor ships with every tested module ; ratchet up, don't silently lower), and why e2e is manual. Refreshed [deploy-checklist.md](monark/app/docs/technical-documentation/deploy-checklist.md) for the new env surface: Sentry (`SENTRY_DSN` / `NEXT_PUBLIC_SENTRY_DSN` + the source-map token) on Render + Vercel with a corrected "monitoring" note, the `BRANDING_*` / `NEXT_PUBLIC_BRANDING_*` white-label vars, and the `pnpm provision:org` CLI. Linked both from [docs/README.md](monark/app/docs/README.md) and fixed [architecture.md](monark/app/docs/technical-documentation/architecture.md)'s stale CI/deploy stack rows.
