- 2026-08-09: `PageHeader` — de-duplicated the back link + title on mobile now that the section shell's scroll-linked detail bar (previous entry) shows the same thing in the chrome. Dropped the inline mobile fallback back-link entirely (the desktop absolute-positioned one, `xl:inline-flex`, is unchanged) ; the inline `<h1>` goes `sr-only xl:not-sr-only` below `xl` _only_ when `backHref` is set — visually hidden (the detail bar already shows it) but still in the accessibility tree, so heading hierarchy / screen-reader navigation is unaffected. `subtitle` and `action` have no chrome equivalent, so they stay visible inline at every breakpoint regardless. Pages with no `backHref` (e.g. `/account`'s tab-level headers, which have no back affordance by design) have no chrome replacement, so their title stays visible inline on mobile too — the hiding is conditional on the exact same test the chrome registration uses, so the two can never drift out of sync. Desktop is untouched. `tsc` + `eslint` + 279 web tests green ; verified with Playwright (computed-style + bounding-rect checks confirming the `sr-only` 1×1px signature on mobile for a `backHref` page, full visibility on desktop, and full visibility on mobile for a no-`backHref` page).
