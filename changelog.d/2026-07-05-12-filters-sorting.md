- 2026-07-05: Filters + sorting — reset buttons restyled to match the data-table config reset. Every filter/sort reset (the all-filters "Reset filters", the per-field "Reset" in each field's sub-menu, and the "Reset sorting" action) now uses the `RotateCcw` reload icon and full-opacity text, matching the columns panel's "Reset layout" row instead of the earlier greyed-out `FilterX` treatment ([filter-menu.tsx](monark/app/services/web/src/components/patterns/filter-menu.tsx), [panels.tsx](monark/app/services/web/src/components/patterns/data-table/panels.tsx)). No behavior change ; the per-field reset was already present in both the desktop field sub-menu and the mobile / collapsed control. `tsc` + `eslint` + 222 tests green ; verified against the `filter-menu-clear` story.
