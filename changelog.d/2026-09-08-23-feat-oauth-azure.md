- 2026-09-08: Added Microsoft (Entra ID) to social sign-in, on top of the GitHub-first provisioning path in `@monark/auth`; `azure` joins [`OAUTH_PROVIDERS`](packages/auth/src/contracts/oauth.ts) with a "Microsoft" label, a brand mark in [oauth-buttons.tsx](<services/web/src/app/(anon)/oauth-buttons.tsx>), and an `[auth.external.azure]` stanza in [supabase/config.toml](supabase/config.toml) (shipped disabled). No change to the callback, provisioning, refusal or account-page logic ; none of it is provider-specific. `azure` is Supabase's own slug for Entra ID and is what we carry end to end; "Microsoft" is only the display label. Operators picking this up should note the tenant decision (the default multi-tenant endpoint accepts work and personal accounts, single-tenant needs an explicit `url`) and that the client secret expires after 24 months at most.
