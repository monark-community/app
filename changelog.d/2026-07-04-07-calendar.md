- 2026-07-04: Calendar — event description is now a WYSIWYG editor. Extracted a standalone, controlled [`RichTextEditor`](monark/app/services/web/src/components/fields/inputs/rich-text-editor.tsx) (value = sanitized HTML, no react-hook-form) out of the RHF-bound `RichTextField` (which now wraps it), exported it from [`@/components/fields`](monark/app/services/web/src/components/fields/index.ts), and swapped the plain description `<Textarea>` in [new-event-popover.tsx](<monark/app/services/web/src/app/(authed)/calendar/new-event-popover.tsx>) for it (toolbar labels from `useFieldStrings().labels.richText`). Descriptions persist as HTML through the unchanged `events.create`/`update` path ; they aren't rendered read-only anywhere yet, so no `RichTextView` wiring was needed. `tsc` + `eslint` green.
