- 2026-05-01: `pnpm dev:tools` CLI for the local dev stack. New [tools/open-tool.ts](monark/app/tools/open-tool.ts) opens (or starts) the auxiliary services we lean on day-to-day so nobody has to memorise port numbers ; cross-platform browser launch (`open` / `start` / `xdg-open` per OS). Tools: `mail` (Inbucket at :54324), `supabase` (Studio at :54323), `api` (Supabase REST at :54321), `db` (Prisma Studio, long-running via `pnpm --filter @monark/db db:studio`), `web` (:3000), `api-server` (:4000), `all` (every URL-based tool). Run with no arg prints the list. New root scripts: `dev:tools`, `dev:tools:mail`, `dev:tools:supabase`, `dev:tools:db`, `dev:tools:all`.
