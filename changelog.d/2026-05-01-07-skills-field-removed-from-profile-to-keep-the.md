- 2026-05-01: Skills field removed from profile to keep the Phase 1 starter Monark-agnostic. Dropped the `User.skills String[]` column ([migration 20260501100000_drop_user_skills](monark/app/packages/db/prisma/migrations/20260501100000_drop_user_skills/migration.sql)), deleted [packages/users/src/contracts/skills.ts](monark/app/packages/users/src/contracts/skills.ts) (canonical SKILLS list), removed the `skills` field from `users.updateProfile` zod schema + `updateProfileData` patch type + `UserProfileUpdatedEvent.changed` enum. UI: stripped the skill toggle row from [profile-section.tsx](<monark/app/services/web/src/app/(authed)/account/profile-section.tsx>) along with its local state + `onToggleSkill` handler. **i18n** (en + fr): removed `account.profile.labels.skills` + the entire `account.profile.skillLabels.*` namespace. Skills will return as an extended module if/when a downstream app needs them.
