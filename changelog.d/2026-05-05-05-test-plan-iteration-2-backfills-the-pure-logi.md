- 2026-05-05: Test plan iteration 2. Backfills the pure-logic unit gaps that don't need a Postgres testcontainer to exercise.
  - **rbac validators extracted + tested.** Pulled `validateRoleKey` / `validateColor` / `validatePermissionList` out of [packages/rbac/src/server/write.ts](monark/app/packages/rbac/src/server/write.ts) into a dedicated [validators.ts](monark/app/packages/rbac/src/server/validators.ts) so they're unit-testable without DB infrastructure ; `write.ts` re-imports from there. New [tests/validators.test.ts](monark/app/packages/rbac/tests/validators.test.ts) — 23 cases covering hex validation (3-digit + 6-digit + reject-on-bad), role-key constraints (length 2–60, lowercase alnum + dash/underscore, no leading/trailing separator, reserved-key rejection for ADMIN + SYSADMIN case-insensitive), and permission-list dedup + order preservation + first-unknown-throws.
  - **Auth event emitters tested.** New [packages/auth/tests/events.test.ts](monark/app/packages/auth/tests/events.test.ts) — 9 cases. Pins the system clock with `vi.useFakeTimers()` so `occurredAt` is deterministic, then verifies each emitter (`emitSignedIn`, `emitSignedOut`, `emitPasswordChanged`) fires the right event shape with the right `triggeredBy` / `scope` / optional `trustedDeviceId` ; closes the loop on cross-package wiring (auth wrappers feed the shared `@monark/common` bus, no auth-local registry).
  - **Test count : 206 → 238 across the workspace, all green.** Workspace typecheck + lint clean ; existing 60 auth tests grew to 69, rbac suite grew from 4 to 28 (placeholder + 4 permission-registry cases + 23 validator cases).
  - **No DB / no SMTP needed for this iteration.** Pure-logic surface is now well-covered ; the next pass is integration suites against testcontainer Postgres (data-layer round-trips + atomic transaction guarantees + FK CASCADE behaviour) per [test-plan.md](monark/app/docs/technical-documentation/test-plan.md). Those land at phase-2 land-time per the plan's phasing.
