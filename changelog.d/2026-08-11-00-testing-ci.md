- 2026-08-11: Testing / CI — raised coverage across the four automation-integration modules, mostly closing their `client.ts` + boot-registration gaps (both classes had sat at 0 % function coverage). **twitter** 67 %→**99 %** lines / 43 %→**100 %** functions: unit tests for the OAuth-1.0a-signed `twitterCall` (over a mocked `safeFetch`) + its X-error-envelope parsing + the credential resolver ([client.test.ts](monark/app/packages/twitter/tests/client.test.ts)), the three action nodes driven through their real `run` with the client mocked ([nodes.test.ts](monark/app/packages/twitter/tests/nodes.test.ts)), and the flag/permission registrations. **discord** 91 %→**100 %** functions (the bot-REST wrapper + the discord.com webhook-URL sender with its SSRF guard, [client.test.ts](monark/app/packages/discord/tests/client.test.ts)), **github** 83 %→**96 %** functions (the REST wrapper + event-type/flag/permission registrations), **chat** 71 %→**79 %** functions (registrations; the Anthropic LLM client stays a known external-API gap). 27 new tests ; floors raised (twitter 90/90/90/80, discord 90/90/90/65, github 90/90/90/60, chat 70/70/70/70) ; full pipeline + merge green. Noted but left alone: `@monark/search` puts its whole server in `server/index.ts`, which coverage excludes as a barrel — so its (passing) router tests aren't attributed ; it'd need a `router.ts` split to be gate-measured.
