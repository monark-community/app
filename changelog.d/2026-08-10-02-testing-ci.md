- 2026-08-10: Testing / CI — repaired the coverage pipeline after the recent module additions and re-gated the new modules. Three new modules had broken the coverage build: `@monark/search` + `@monark/wiki` were missing the `@vitest/coverage-v8` devDep, and `services/api` was missing `@monark/achievements` + `@monark/wiki` deps its generated app-router imports (so `test:coverage` couldn't resolve them) — same missing-workspace-dep class as the earlier calendar→users fix. Hardened [merge-coverage.ts](monark/app/tools/merge-coverage.ts): `formatPct` no longer crashes on a metric-less package (istanbul reports `"Unknown"` for a zero-total metric), and a package that emits an empty `coverage-final.json` (a no-test barrel like `branding`/`search` run with `--passWithNoTests`) is now skipped rather than tripping the "produced coverage but has no floor" gate. Added floors for the newly-covered modules (`achievements`, `twitter`, `wiki`) at ~5pts below their measured baselines. Net: the merge gate is green again across 26 packages. Then closed the two biggest gaps the assessment surfaced: **`integration-kit`** (the shared integration substrate — REST client, HMAC webhook verify + inbound-handler factory, the connection-secret router) was wired for coverage (scripts/config/deps) and taken 12 %→**100 %** with unit tests (`rest`, `webhook-inbound`) + a connection-router integration spec ; **`achievements`** went 52 %→**91 %** lines / 39 %→**89 %** functions via a full tRPC router integration suite (config + rule CRUD, event-type catalog, gallery/summary/progress with seeded awards, RBAC + NotFound paths) plus boot-registration unit tests — the award engine itself stays covered by `engine.test.ts`. Both floored accordingly (`integration-kit` 90/90/85/85, `achievements` 85/85/80/70). 31 new tests ; full pipeline + merge green.
