- 2026-09-08: Tooling — **migrate off the deprecated `app-id` input on `create-github-app-token`**. GitHub deprecated `app-id` in favor of `client-id` on the action (still functional, just warns). Both [notify-docs.yml](.github/workflows/notify-docs.yml) and [changelog-compile.yml](.github/workflows/changelog-compile.yml) now pass `client-id` instead, reading it from new `DOCS_SYNC_APP_CLIENT_ID` / `CHANGELOG_APP_CLIENT_ID` repository secrets (the App's Client ID, a separate field from its numeric App ID) rather than reusing the old `*_APP_ID` secrets.
