- 2026-07-05: Data tables & forms — column-driven form order, skeleton headers, shared chips + MultiSelect. (1) **Shared chip primitives** ([components/ui/chip.tsx](monark/app/services/web/src/components/ui/chip.tsx)): `Chip` (optional `onRemove` so it is removable-or-not, `leading`, `disabled`, `tone`), `AddChip` (dashed "add new" empty-state affordance), and `ChipList` (bordered wrap container) ; `fields/inputs/chips.tsx` now re-exports them so `MultiSelectField`/`RelationField` keep working unchanged. (2) **Standard controlled `MultiSelect`** ([components/patterns/multi-select.tsx](monark/app/services/web/src/components/patterns/multi-select.tsx)): one component covering fixed options (industries) and free tags (keywords — type + Enter/comma creates a normalized chip, Backspace on empty removes the last), built on `ChipList`/`Chip`/`AddChip`. An option can carry an optional `href` (+ `hrefNewTab`) so its selected chip's label links out while the dropdown row stays plain — restoring the project form's industry chips deep-linking to `/data/industries?industry=<id>` in a new tab (so following one never discards the unsaved form). (3) **Skeleton headers until layout resolves**: `useDataTableLayout` now exposes `hydrated` ; the `DataTable` keeps headers _and_ body in a skeleton until the persisted column order/visibility loads from `localStorage`, so headers never flash in the default order then jump ([use-data-table-layout.ts](monark/app/services/web/src/components/patterns/data-table/use-data-table-layout.ts), [data-table.tsx](monark/app/services/web/src/components/patterns/data-table/data-table.tsx)). The skeleton gate combines the parent's `hydrated` flag with the table's own `mounted` mount-effect flag (`configResolved = layout.hydrated || mounted`) so it can never wedge : `mounted` always flips on the first post-paint commit even if the parent's hydration flag never reaches the table (a suspended / memoized parent, a remount race), while in the normal path both flip in the same batched re-render so the resolved order still paints flash-free. (4) **Projects table gains URL + Keywords columns** ; keywords input is now the chip `MultiSelect` (≤40-char lowercase, max 50) ; **Description is never a selectable column and is always last in the form** ([projects-list.tsx](<monark/app/services/web/src/app/(authed)/data/projects/projects-list.tsx>), [project-form.tsx](<monark/app/services/web/src/app/(authed)/data/projects/project-form.tsx>)). (5) **Form field order follows the table's configured column order** via a `columnId → formKey` map (`COLUMN_TO_FIELD`) + `fieldOrder` prop, reacting live to `TableTools` reordering, with title/slug pinned first and description last ; all fields always render regardless of column visibility. (6) **`PageSection` adopted in the project + industry data forms** for consistent section chrome, with `DirtyFormBar` kept outside the `@container` so panel anchoring is unaffected. `tsc` + `eslint` + 222 tests + i18n parity (1312 keys × en/fr) all green.
