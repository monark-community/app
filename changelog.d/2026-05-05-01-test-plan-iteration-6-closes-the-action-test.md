- 2026-05-05: Test plan iteration 6. Closes the action-test gap, doubles the integration coverage, and lands the bulk of the e2e backfill.
  - **Final action file covered.** [tests/actions/admin-org-actions.test.ts](monark/app/services/web/tests/actions/admin-org-actions.test.ts) — 15 cases for `adminUploadOrgLogoAction`. Mocks `sharp` with a chainable stub + the Supabase admin Storage client. Covers happy ok with `?v=<ts>` cache-buster + canonical `org-logos/<orgId>/<ts>.webp` path, missing orgId / file / wrong MIME / oversized file each → their error code, all three allowed MIMEs (jpeg/png/webp), no-session → notAuthenticated, adminGet UNAUTHORIZED + FORBIDDEN both → `forbidden`, adminGet unknown error → `upstream`, sharp throw on corrupt image → `invalidType`, Storage upload fail → `upstream` (no adminUpdate fires), adminUpdate fail after successful upload → `upstream` (orphan flagged in runbook), cache-buster reaches the adminUpdate call. Every action file in `services/web/src/app/.../actions.ts` now has a test suite.
  - **Two more integration suites against the testcontainer Postgres.** Both use the same shape : workspace dep on `@monark/test-utils`, dedicated `vitest.integration.config.ts` referencing `@monark/test-utils/global-setup`, opt-in `pnpm --filter <pkg> test:integration` script.
    - [packages/users/tests/integration/data.test.ts](monark/app/packages/users/tests/integration/data.test.ts) — 17 cases. Round-trip CRUD : `findById`/`findByEmail` null + populated, `updateProfileData` (display name, bio, locale, avatar/banner — including the null-clears-vs-undefined-leaves rule), `updateEmail` rotation + unique-constraint rejection on duplicate, `setDeletedAt` for both grace start + cancel. List : excludes anonymized rows ending in `@monark.invalid`, case-insensitive search across email + displayName, cursor pagination + last-page nullCursor, every status filter (active / disabled / pending-deletion), `emailVerified` tri-state, `joinedAfter` filter, ordering by `createdAt desc, id desc`, composing all four filters at once.
    - [packages/organizations/tests/integration/data.test.ts](monark/app/packages/organizations/tests/integration/data.test.ts) — 24 cases. `findById` / `findBySlug` round-trips. `countActiveOrganizations` zero / live-only / multi-row. `findOnlyActiveOrganization` returns null on 0 + null on >1 + the singleton on 1 + ignores soft-deleted. `createOrganizationRow` happy + idempotent on slug + null defaults. `isMember` honors `leftAt`. `findOrgsForUser` joinedAt-ASC ordering, excludes left memberships, excludes soft-deleted orgs. `listOrganizationsForAdmin` excludes soft-deleted + case-insensitive search across displayName + slug + cursor pagination. **`updateOrganizationForAdmin` slug rotation** : updates simple fields without touching the redirect table, rotates slug + records 90-day redirect with the right expiry window, rejects collisions, no-op same-slug doesn't record a redirect, idempotent on multiple rotations through the same old slug, transaction-rolls-back on slug collision (displayName change reverts), throws on missing org id.
  - **Three new e2e specs** wrapping up the user-guide flows :
    - [tests/e2e/signup-confirm.spec.ts](monark/app/services/web/tests/e2e/signup-confirm.spec.ts) — full sign-up → poll Inbucket → click confirmation link → land on `/`. Branches on auto-confirm (skips the email step) vs verification-required. Plus a "resend triggers a second email" case that distinguishes by Inbucket message id.
    - [tests/e2e/password-change.spec.ts](monark/app/services/web/tests/e2e/password-change.spec.ts) — sign in → `/account/security` → password modal → rotate → sign out → sign in with new → rotate back to original. Plus an `invalidCurrentPassword` toast assertion + a weak-password Submit-stays-disabled assertion.
    - [tests/e2e/account-deletion.spec.ts](monark/app/services/web/tests/e2e/account-deletion.spec.ts) — sign in → typed-email confirmation request-deletion → land on signin with banner → sign back in → grace-lockdown sidebar (only profile + danger visible) → cancel → sidebar restores all 4 tabs. The hard-delete path stays out (destructive ; covered by the action-tests).
  - **Workspace effect.** New test count : **~330 → ~407 cases declared** across the workspace (+15 admin-org-actions, +17 users-data integration, +24 organizations-data integration, +21 e2e cases across 3 specs). Workspace `pnpm typecheck` + `pnpm lint` still green across all 14 packages including the new integration suites in `@monark/users` and `@monark/organizations`. e2e specs stay gated by `E2E_FULL_STACK=1` so smoke runs aren't disturbed.
  - **Operator handoff** : `pnpm install` (lockfile is now stale through 6 iterations of changes — first run will need `--frozen-lockfile=false`). `pnpm --filter @monark/users test:integration` + `pnpm --filter @monark/organizations test:integration` to run the new integration suites (Docker required for the Postgres container ; CI's ubuntu-latest is fine, Windows local needs Docker Desktop). `E2E_FULL_STACK=1 pnpm test:e2e` against `supabase start`'s local stack with the `E2E_USER_*` + `E2E_RESET_EMAIL` env vars set.
