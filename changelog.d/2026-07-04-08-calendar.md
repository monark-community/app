- 2026-07-04: Calendar — event description editor fills the popover height on desktop. The [RichTextEditor](monark/app/services/web/src/components/fields/inputs/rich-text-editor.tsx) gained an opt-in `fill` prop : it becomes a flex column (toolbar fixed, body `flex-1` + scroll, editable stretched so clicking anywhere in the empty space focuses) that grows to its parent's resolvable height instead of sizing to `minHeight`. The event popover passes `fill={!isMobile}` in [new-event-popover.tsx](<monark/app/services/web/src/app/(authed)/calendar/new-event-popover.tsx>) so on desktop the description matches the (grid-stretched) taller metadata column ; mobile keeps the min-height so the full-screen dialog scrolls naturally. New `event-desc-fill` screenshot story verifies both viewports ; `tsc` + `eslint` green, 222 tests pass.
