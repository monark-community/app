- 2026-07-04: Calendar — day view merges per-calendar columns into one color-coded grid + standardized chips. The day view no longer reserves one column (and a name header) per calendar ; every visible calendar's events are merged into a single [`DaySchedule`](monark/app/packages/calendar/src/client/ui/day-schedule.tsx) column, colored per-event from the calendar's color and placed side-by-side only where they actually overlap (the existing overlap-cluster layout), so empty calendars cost no horizontal space and the calendar-name header row is gone ([day-view.tsx](<monark/app/services/web/src/app/(authed)/calendar/day-view.tsx>) : `deriveColumns`/per-calendar `eventsMap` replaced by flat `timedEvents`/`allDayEvents` + a `colorMap`, drag/pending/ghost overrides rewritten to match by id across the merged list ; a slot click defaults to the first visible calendar since a column no longer implies one). The mobile-only calendar chips were extracted into a shared [`CalendarChip`](<monark/app/services/web/src/app/(authed)/calendar/calendar-sidebar.tsx>) (rounded calendar-colored border, active = tinted / inactive = dimmed, color dot + name toggle, trailing `…` edit/archive menu) and now render on the desktop sidebar too — replacing the old checkbox list — with the long-press bottom-sheet dropped in favor of the chip's `…` menu on both surfaces. New `showCalendar`/`hideCalendar` i18n (en + fr) ; a `calendar-chips` screenshot story verifies the states. `tsc` + `eslint` + `check:i18n` green.
