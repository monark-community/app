- 2026-08-09: Global search / fuzzy — **the palette is now typo-tolerant and relevance-ranked** (trigram, `pg_trgm`). Migration `20260809000000_search_trigram_indexes` enables `pg_trgm` + adds GIN `gin_trgm_ops` indexes on the searched text columns (wiki title + `contentText`, kanban title + `descriptionText`, calendar title + description, `DataRecord.title`, `Automation.name`). New raw-SQL helpers in [@monark/db](monark/app/packages/db/src/trigram.ts) — `trigramMatch` (a `WHERE` that matches on `ILIKE` substring OR `word_similarity > threshold`) + `trigramOrder` (rank by best similarity across columns) — since Prisma can't express similarity. Each source's data-layer search was rewritten from `contains` to raw SQL using them (`searchPages`, `searchCards`, a light `searchCalendarEventsForPalette`, `searchAutomations`, `searchRecordsAcrossModels` — the last keeps its row-level access via the raw `recordRoleAccessSql`). So "runbok" now finds "Runbook", and each module's group is ordered by closeness. Per-group ranking (grouped by module, as before) — cross-source unified ranking + Postgres FTS for long bodies stay as follow-ups. The trigram indexes are raw-SQL (like `DataRecord_data_gin`) so they read as phantom drift in a future `migrate dev` and must be stripped. Verified: a typo-match integration test + all module search suites green against a real DB.
