- 2026-09-08: Achievements — **the page owns its bounded-scroll chrome**, so a long catalog is reachable again. `/achievements` has no `layout.tsx` of its own and `<body>` is globally `overflow-y-hidden`, so the page had no scroll container at all and everything past the fold was simply unreachable ; the gallery also sat flush against the nav rail and the AppBar with no padding. [achievements/page.tsx](<services/web/src/app/(authed)/achievements/page.tsx>) now wraps its content in the same `calc(100dvh - 57px)` flex column plus `overflow-y-auto` main that `/automation`, `/calendar`, `/kanban` and `/wiki` already use (57px being the AppBar's 56px row plus its 1px hairline), with a padded `max-w-6xl` container inside. Hand-rolled rather than mounted on [SectionShell](services/web/src/components/section-shell.tsx), matching the other standalone section pages ; only `/admin`, `/account` and `/data` mount the shell today.
