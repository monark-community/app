- 2026-07-05: Filters + sorting — "reset" wording, per-field reset, and a sorting reset. Renamed the filter menu's all-clear from "Clear filters" to **"Reset filters"** to match the data-table config's "Reset layout" verb (`filters.clearAll` text, en + fr). Added a **per-field reset** so a single filter can be cleared in one click instead of deselecting each value : [`DesktopFilterControl`](monark/app/services/web/src/components/patterns/filter-menu.tsx) appends a "Reset" `DropdownMenuItem` (kept-open) and `FilterFieldControl` a "Reset" button, both shown only while that field is active, driven by the existing `clearFilter` + a new optional `FilterMenuLabels.resetField` (`filters.resetField` = "Reset" / "Réinitialiser"). Added a **"Reset sorting"** action at the bottom of the [`SortingPanel`](monark/app/services/web/src/components/patterns/data-table/panels.tsx) that clears every sort field at once (`setSorting([])`), gated on there being active sorts, via a new optional `DataTableSortLabels.reset` (`table.sortReset`, en + fr). Threaded the two new labels through all filter callers (projects, industries, users, webhooks) and every sort-label builder (those four + organizations, roles, deliveries, dev gallery). `filter-menu-clear` screenshot story updated (mobile shows the per-field "Reset" on the active field + the "Reset filters" footer). `tsc` + `eslint` + `check:i18n` + 222 tests green.
