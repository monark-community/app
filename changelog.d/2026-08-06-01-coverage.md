- 2026-08-06: Coverage — **floored the new automation-integration modules on the develop gate + made discord's tests run in CI.** The now-live `develop` coverage gate requires a `THRESHOLDS` floor per package that emits coverage, which the freshly-landed integrations lacked. Added node-level unit tests mirroring github's: **telegram** ([nodes.test.ts](monark/app/packages/telegram/tests/nodes.test.ts)) drives all five action-node executors + helpers through a mocked `telegramCall` (100 % on the node files), and **discord** ([nodes.test.ts](monark/app/packages/discord/tests/nodes.test.ts)) covers all six nodes + helpers. **discord's `test:coverage` script was missing** — it had a `test` script but nothing the coverage job runs, so its unit tests never ran in CI ; added the script + `@vitest/coverage-v8`. Floors in [merge-coverage.ts](monark/app/tools/merge-coverage.ts): `package/discord` `{85/85/60/55}` (unit-only, ~91 % lines) and `package/telegram` `{50/50/60/65}` — the latter set conservatively off the unit lower bound because telegram's inbound webhook + connection router are integration-covered, so its merged number runs well above (ratchet after the first merged CI run publishes it). `@monark/integration-kit` stays coverage-skipped: it's a shared library exercised indirectly by its consumers, so direct kit tests are a follow-up rather than a ~12 %-floor on the dashboard. telegram 14 + discord 12 unit tests green ; typecheck clean.
