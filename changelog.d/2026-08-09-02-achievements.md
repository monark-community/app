- 2026-08-09: Achievements — **uploaded image badges** (in addition to the emoji). An achievement gains an optional `iconFileId` (migration `20260809020000_achievement_icon_file`) — a `@monark/files` `StoredFile` in a new **public** `achievement-icons` bucket (`ensureAchievementIconsBucket`, provisioned at api boot ; `ACHIEVEMENT_ICONS_BUCKET` + allowed image MIME types in [contracts/types.ts](monark/app/packages/achievements/src/contracts/types.ts)). The [router](monark/app/packages/achievements/src/server/router.ts) resolves each `iconFileId` to a public URL (`resolveIconUrls` → `getPublicUrl`, best-effort — falls back to the emoji when storage is unset) and returns `iconUrl` on `list` / `catalog` / `myAwards` / `myProgress` / create / update. The admin editor ([achievements-manager.tsx](<monark/app/services/web/src/app/(authed)/admin/achievements/achievements-manager.tsx>)) gains an **Upload image** control (the shared `useFileUpload` hook → the achievement-icons bucket) with a live preview via `files.downloadUrl` ; the badge renders the image when present (emoji fallback) in both the admin list and the user [gallery](<monark/app/services/web/src/app/(authed)/achievements/achievements-gallery.tsx>). `@monark/achievements` now depends on core `@monark/files`. typecheck + lint + `check:i18n` / `check:tiers` / `check:modules` green ; 7 unit + 4 integration unchanged.
