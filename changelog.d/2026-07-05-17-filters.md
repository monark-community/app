- 2026-07-05: Filters — a "Clear filters" action at the bottom of every filter menu. Resets every active filter to its neutral value (select → default, multi-select → empty, text / date → empty) in one click. Added [`clearAllFilters`](monark/app/services/web/src/components/patterns/filter-menu.tsx) + a shared `FilterClearButton`, and an optional `FilterMenuLabels.clearAll` label (omit to hide). Rendered in all four filter surfaces : the desktop `FilterMenu` dropdown (a `DropdownMenuItem` below a separator, disabled while nothing's active, keeps the menu open so the cleared state is visible), the mobile filter modal footer, and the `TableTools` collapsed-tools drill-in popover + mobile dialog. Wired the four list screens (projects, industries, users, webhooks) off a new `filters.clearAll` i18n key ("Clear filters" / "Effacer les filtres", en + fr). New `filter-menu-clear` screenshot story (mobile modal verified). `tsc` + `eslint` + `check:i18n` + 222 tests green.
