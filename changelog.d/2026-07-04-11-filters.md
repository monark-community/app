- 2026-07-04: Filters — multi-value fields + an opaque option-search. Filter fields that were single-select radio pickers are now multi-select where several values make sense : project **status** + **industry** and user **role** + **status** each take a set now (checkbox lists ; any-of / OR match). Projects' `list` procedure input widened from `publicStatus` / `industryId` to `publicStatuses` / `industryIds` arrays, matched by Prisma `in` in [listProjects](monark/app/packages/projects/src/server/data.ts) + [the router](monark/app/packages/projects/src/server/index.ts) ; the users list procedure already accepted `roleIds` / `statuses` arrays, so [users-list.tsx](<monark/app/services/web/src/app/(authed)/admin/users/users-list.tsx>) just passes the sets through (the synthetic `pending` status still drives invite visibility ; it's filtered out of the real user-status query). Also fixed the sticky field-search box letting scrolled option rows show through it in every surface (the desktop submenu, the collapsed-tools drill-in Popover, and the mobile filter dialog). The shared [`Input`](monark/app/services/web/src/components/ui/input.tsx) is `bg-transparent`, so even a bar with a solid background was a see-through window onto the results ; the fix gives the search input itself an opaque `bg-background`, and on the desktop submenu drops the `DropdownMenuSubContent` padding (`p-0`) so the sticky bar sits flush at the top edge + full width with its option list re-padded below. The mobile / collapsed search also carries its host surface colour (`bg-popover` in a Popover, `bg-background` in a Dialog) via a new `searchSurfaceClassName` on `FilterFieldControl` in [filter-menu.tsx](monark/app/services/web/src/components/patterns/filter-menu.tsx). `pnpm gen`, web `tsc` + `eslint` + `check:i18n` green ; 222 web tests pass.
