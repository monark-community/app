- 2026-07-04: Calendar event popover + WYSIWYG toolbar polish. Reordered the event popover's right column to Type → Date → Start/End times → Participants → Location → Calendar → Reminders (top-down) and widened the popover (`w-165` → `w-190`) so it all fits ([new-event-popover.tsx](<monark/app/services/web/src/app/(authed)/calendar/new-event-popover.tsx>)) ; each reminder row now reads "N `[unit]` before" (new `reminderBefore` i18n, en + fr) so it's clear the offset is _before_ the event. Reworked the shared [RichTextEditor](monark/app/services/web/src/components/fields/inputs/rich-text-editor.tsx) toolbar (so every WYSIWYG surface benefits) : the H1/H2 buttons collapse into a single text-style dropdown offering Paragraph + Heading 1–4 (StarterKit `heading.levels` widened to `[1,2,3,4]`, `setParagraph` / `setHeading` for idempotent picks) ; the always-present Undo / Redo buttons are gone ; the Unlink button now renders only when the caret sits on a link instead of showing perpetually disabled. New `richText.{heading3,heading4,paragraph,textStyle}` i18n (en + fr) ; dropped the now-unused `richText.{undo,redo}` keys. Verified via the `event-desc-fill` screenshot story ; `tsc` + `eslint` + `check:i18n` green, 222 tests pass.
