- 2026-07-04: Calendar — mobile date picker is a popover, not a full-screen modal. The mobile toolbar date button now opens the day picker in a `Popover` anchored right under it ([calendar-shell.tsx](<monark/app/services/web/src/app/(authed)/calendar/calendar-shell.tsx>)) instead of a `mobileFullScreen` `Dialog`, which read as oddly placed. Same react-day-picker `Calendar` + URL-driven `navigateTo`, just re-hosted ; the full-screen dialog + its header were removed.
