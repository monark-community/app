- 2026-07-04: Calendar — mobile date button opens a full-screen date picker, toolbar reordered. On mobile the toolbar's plain date label is now an outlined `CalendarDays` button that opens a `mobileFullScreen` [Dialog](monark/app/services/web/src/components/ui/dialog.tsx) holding a single-select [Calendar](monark/app/services/web/src/components/ui/calendar.tsx) (localized fr/enUS ; pick a day → `navigateTo` + close) ; desktop keeps the static label. The mobile control order flips to `[Today] [date] [‹] [›]` (chevrons to the right of the date) via flex `order` utilities, desktop unchanged, in [calendar-shell.tsx](<monark/app/services/web/src/app/(authed)/calendar/calendar-shell.tsx>) ; new `calendar.shell.goToDate` i18n (en + fr).
