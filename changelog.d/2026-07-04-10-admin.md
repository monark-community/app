- 2026-07-04: Admin — projects/industries separation + industry deep-links + panel banner bleed (first pass of a larger admin cleanup). (1) **Split project vs industry events into independent categories**: industry RBAC permissions moved from `category: "projects"` to `category: "industries"` ([packages/projects/src/server/permissions.ts](monark/app/packages/projects/src/server/permissions.ts)) so the role editor lists them as their own group (new `admin.rbac.editor.categories.industries` label, en + fr) ; industry webhook events now register under a separate `registerEventTypes("industries", …)` group ([event-types.ts](monark/app/packages/projects/src/server/event-types.ts)) so the subscription picker shows Projects and Industries as two categories (event `type` strings unchanged, so no codegen / no consumer break). (2) **Industry deep-links**: a project's linked-industry badges are now links to `/data/industries?industry=<id>` — same-tab in the projects data table ([projects-list.tsx](<monark/app/services/web/src/app/(authed)/data/projects/projects-list.tsx>)), new-tab from the project form's selectable chips ([project-form.tsx](<monark/app/services/web/src/app/(authed)/data/projects/project-form.tsx>)) so following one never discards the unsaved form. (3) **Full-bleed banner in the user panel**: `UserBanner` gained a `bleed="container"` mode that cancels a detail panel's `px-6 py-6` gutter (`-mx-6 -mt-6`, content row re-padded) so the admin user banner spans the panel edge-to-edge while the fields below stay padded ([user-banner.tsx](monark/app/services/web/src/components/user-banner.tsx)) ; `UserDetail` passes it in panel mode. Verified banner spans the full panel width headlessly. `eslint` + `tsc` green (web + projects package). Still to come: a shared grouped-multi-select component (refactoring RBAC + webhooks + notifications onto it), per-event notification preferences, and a WYSIWYG industry description.
