- 2026-05-05: Test plan iteration 5. Three layers in this push : remaining server-action coverage, the testcontainers Postgres harness for integration tests, and the e2e backfill kickoff.
  - **Server-action coverage : 76 new cases across 5 files.**
    - [tests/actions/signup-actions.test.ts](monark/app/services/web/tests/actions/signup-actions.test.ts) — 14 cases. Happy redirect to `/signup/check-email` + URL-encoded email, auto-confirm path lands `/account`, `emailInUse` for TRPC CONFLICT, `weakPassword` for BAD_REQUEST, `fallback` for unknown TRPC errors + non-TRPC throws, accept-language → localePreference, runtime app URL forwarding, optional displayName, notifySignedIn + consumePending fire only on auto-confirm, both swallow failures cleanly.
    - [tests/actions/signin-totp-actions.test.ts](monark/app/services/web/tests/actions/signin-totp-actions.test.ts) — 10 cases. TOTP mode : valid code redirects to `/account` with notifySignedIn, expired pending cookie + missing access token both → `expired`, verifyCode `!ok` and throw both → `invalidCode`, null trustedDeviceId path, swallow notifySignedIn failure. Recovery mode : uses verifyRecoveryCode (not verifyCode), recovery-code rejection → `invalidCode`, recovery procedure throw → `invalidCode`.
    - [tests/actions/reset-password-actions.test.ts](monark/app/services/web/tests/actions/reset-password-actions.test.ts) — 15 cases. Happy redirect to `/signin?passwordReset=1`, `noSession` for getUser error / no email / no access token, full TOTP gate (not-supplied / too-short / verify-fails / verify-throws / verify-succeeds), `weakPassword` on `!ok` + on throw, `upstream` on Supabase update fail, swallows notifyPasswordChanged failure on already-rotated success, no TOTP requirement for non-enrolled, totp.status throw degrades open (transient API failure can't brick everyone's reset), email passed into strength check.
    - [tests/actions/check-email-actions.test.ts](monark/app/services/web/tests/actions/check-email-actions.test.ts) — 13 cases. `resendConfirmationAction` : happy + remaining count, `missingEmail` for empty, runtime app URL forwarded, `upstream` on throw, propagates `alreadyVerified` + `exhausted` (with retry-after) error codes. `verifyOtpAction` : redirects to `/account` on valid signup OTP, `invalidCode` for empty email / token / Supabase error / no session, trims whitespace, swallows markOwnEmailVerified failure (auth.users is the source of truth).
    - [tests/actions/account-other-actions.test.ts](monark/app/services/web/tests/actions/account-other-actions.test.ts) — 24 cases for `updateLocaleAction` + `requestEmailChangeAction` + `verifyEmailChangeOtpAction` + `requestAccountDeletionAction` + `cancelAccountDeletionAction`. Locale : full propagation (API + Supabase + cookie), no-session degrades to cookie-only, swallows API + Supabase failures. Email change : happy non-TOTP, `upstream` for malformed email, `sameEmail` case-insensitive, `invalidCurrentPassword`, `totpRequired` + `invalidTotpCode`, Supabase `email_exists` / `email_address_invalid` → `emailInUse`, other Supabase errors → `upstream`, normalises trim + lowercase. Email change OTP verify : `missing` for empty inputs, `notAuthenticated` for no user, `invalidCode` after both verify attempts fail, `pendingOtherSide:true rotated:false` for half-done rotation, syncs shadow row + signs out for full rotation, falls back to old-email verify when new-email fails. Deletion : happy ok with `deletionCompletesAt`, `notAuthenticated` / `invalidConfirmation` / `upstream`, case-insensitive + whitespace-tolerant email match, sign-out doesn't fire on API failure. Cancel deletion : happy ok, no-session → ok:false, throw → ok:false.
  - **Testcontainers Postgres harness.** New workspace package [@monark/test-utils](monark/app/packages/test-utils) with two exports : [`./db`](monark/app/packages/test-utils/src/db.ts) (boots a fresh Postgres testcontainer + runs `pnpm db:migrate` against it + sets `DATABASE_URL`/`DIRECT_URL` env vars + exposes a `truncate(prisma, tables)` helper for between-test cleanup) and [`./global-setup`](monark/app/packages/test-utils/src/global-setup.ts) (vitest globalSetup wiring). Lazy-imports `@testcontainers/postgresql` so the typecheck stays green even before `pnpm install` resolves the dep on a fresh clone. Per-package opt-in via a separate `vitest.integration.config.ts` + a `test:integration` script ; default `pnpm test` doesn't require Docker.
  - **First integration test PoC** at [packages/rbac/tests/integration/role-crud.test.ts](monark/app/packages/rbac/tests/integration/role-crud.test.ts) — 16 cases hitting real Prisma queries. `findRoleById` (null + populated path with permissions). `createCustomRole` (creates role + writes permission rows + empty-list path + unique-constraint violation on duplicate (key, organizationId)). `listRolesForOrg` (custom roles + built-in ADMIN, SYSADMIN excluded, custom-roles-of-other-orgs hidden). `updateRolePatch` (atomic name+description+color update, replace-all permissions semantic, omit-permissions leaves them untouched). `countActiveAssignmentsForRole` (zero-count + revoked-row exclusion). `deleteRoleRow` (hard delete + RolePermission cascade). Plus a smoke check for the shared `truncate` helper.
  - **e2e backfill kickoff.** New [tests/e2e/helpers/inbucket.ts](monark/app/services/web/tests/e2e/helpers/inbucket.ts) — Inbucket REST API client (Supabase's local stack ships Inbucket, port 54324) with `clearInbucket(email)`, `waitForLatestEmail({ to, subjectMatches, timeoutMs })`, `extractFirstUrl(body, pattern)`, `extractOtp(body, pattern)`. Mirrors the existing `mailpit.ts` helper's API. New [tests/e2e/forgot-password.spec.ts](monark/app/services/web/tests/e2e/forgot-password.spec.ts) covers the full recovery loop (request reset → poll Inbucket → click link → submit new password → sign in with rotated password). New [tests/e2e/signin-happy-path.spec.ts](monark/app/services/web/tests/e2e/signin-happy-path.spec.ts) covers happy sign-in + sign-out via the user-menu drawer + the invalid-credentials inline error. Both gated by `E2E_FULL_STACK=1` so smoke runs (`auth-routing.spec.ts`) keep passing on bare CI.
  - **Effect on totals.** Workspace test count up to ~330 cases declared (238 from earlier iterations + 76 new actions + 16 integration). Workspace `pnpm typecheck` + `pnpm lint` clean across all 14 packages including the new `@monark/test-utils`. The default `pnpm test` task fails on `web` and `@monark/test-utils` until `pnpm install` picks up the new devDeps ; install-time, resolves once the lockfile commits.
  - **Operator handoff.** `pnpm install` (first run will need `--frozen-lockfile=false` because the lockfile is stale) then commit the updated lockfile. `pnpm --filter @monark/rbac test:integration` runs the integration suite (requires Docker Desktop on Windows ; CI's ubuntu-latest has Docker built in). `E2E_FULL_STACK=1 pnpm test:e2e` runs the full e2e backfill against `supabase start`'s local stack ; pre-seed a test user + set `E2E_USER_EMAIL` / `E2E_USER_PASSWORD` / `E2E_RESET_EMAIL` env vars first.
