# Prisma CLI reads env vars from the schema's package (packages/db).
# Duplicate values from services/api/.env here so `prisma migrate` / `prisma generate` / `prisma studio`
# pick them up when invoked from this package.

DATABASE_URL=
DIRECT_URL=
